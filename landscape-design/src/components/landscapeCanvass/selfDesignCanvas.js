import React from "react";

const SelfDesignCanvas = (props) => {
  return (
    <div className="h-full w-full flex bg-slate-100 justify-center items-center">
      this is where I will be adding my three.js canvas
    </div>
  );
};

export default SelfDesignCanvas;
